version: '3.8'
services:
  dyma-postgresql:
    image: postgres:17
    container_name: dyma-postgre-production
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - "5424:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
